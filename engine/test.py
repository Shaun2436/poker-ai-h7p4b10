# import json
# from state import GameState

# s = GameState.from_seed(1)
# counts = s.deck_remaining_counts()

# print("Python dict iteration order (first 15 keys):")
# print(list(counts.keys())[:15])

# print("\nJSON default dumps (first ~120 chars):")
# print(json.dumps(counts)[:120])

# print("\nJSON with sort_keys=True (first ~120 chars):")
# print(json.dumps(counts, sort_keys=True)[:120])
